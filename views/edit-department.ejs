<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Department | UAA System</title>

    <!-- Tailwind -->
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gradient-to-br from-blue-100 to-blue-200 min-h-screen flex items-center justify-center">

    <div class="bg-white/80 backdrop-blur-lg shadow-xl rounded-xl p-10 w-2/3 border border-blue-200">

        <h1 class="text-3xl font-bold text-blue-700 mb-6">Edit Department</h1>

        <% if (message) { %>
            <p class="mb-4 text-green-600 font-semibold"><%= message %></p>
        <% } %>

        <form action="/admin/departments/<%= department._id %>/update" method="POST" class="space-y-6">

            <!-- NAME -->
            <div>
                <label class="block text-blue-700 font-medium mb-1">Department Name</label>
                <input 
                    type="text" 
                    name="name"
                    value="<%= department.name %>"
                    class="w-full px-4 py-2 rounded-lg border border-gray-300"
                    required
                />
            </div>

            <!-- TYPE -->
            <div>
                <label class="block text-blue-700 font-medium mb-1">Department Type</label>
                <select name="type" class="w-full px-4 py-2 rounded-lg border border-gray-300" required>
                    <option value="UG" <%= department.type === "UG" ? "selected" : "" %>>UG</option>
                    <option value="PG" <%= department.type === "PG" ? "selected" : "" %>>PG</option>
                    <option value="Research" <%= department.type === "Research" ? "selected" : "" %>>Research</option>
                </select>
            </div>

            <!-- ADDRESS -->
            <div>
                <label class="block text-blue-700 font-medium mb-1">Address</label>
                <textarea
                    name="address"
                    class="w-full px-4 py-2 rounded-lg border border-gray-300"
                    required
                ><%= department.address %></textarea>
            </div>

            <button 
                type="submit"
                class="w-full py-3 bg-blue-600 text-white font-semibold rounded-lg hover:bg-blue-700"
            >
                Update Department
            </button>

        </form>

    </div>

</body>
</html>
